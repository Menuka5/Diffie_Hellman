1. First run Command Prompt
2. Compile DH.java using cmd command
3. When you run the program as java DH private_key, it should print the public key on the console.
4. When you run the program as java DH private_key public_key, it should print the session key on the console.